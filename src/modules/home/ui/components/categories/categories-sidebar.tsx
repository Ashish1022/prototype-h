"use client"

import { useRouter } from "next/navigation";

export const CategoriesSidebar = ({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void }) => {

    const router = useRouter();

    return (
        <div>

        </div>
    )
}